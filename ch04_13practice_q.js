/*
Create an array to store companies -> "Bloomburg", "Microsoft", "Uber", "Google", "IBM", "Netflix" 
- Remove the first company from array
- Remove the Uber & Add Ola in its place
- Add Amazon at the end
*/
let Companies = ["Bloomberg", "Microsoft", "Uber", "Google", "IBM", "Netflix"];

// -Remove the first company from array
// Companies.slice(1);
// Companies.shift();      //'Bloomburg' delete

// -Remove the Uber & Add Ola in its place
// Companies.splice(2, 1, "Ola");

// -Add amazon at the end
Companies.push("amazon");